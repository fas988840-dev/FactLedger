"""Shared pytest fixtures for the JONARAI backend.

Currently empty — every test in this suite is self-contained. Kept as
a module so pytest discovers the ``tests/`` package.
"""

from __future__ import annotations
